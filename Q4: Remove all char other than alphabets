#include<bits/stdc++.h>
using namespace std;
int main(){
    string s,ans="";
    cin>>s;
    for(int i=0;i<s.size();i++){
        int val=s[i];
        if((val>=65 && val<=90)||(val>=97 && val<=122)){
            ans+=s[i];
        }
    }
    cout<<ans;
}
