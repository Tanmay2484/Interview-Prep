#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin >> s;
    
    int n = s.size();
    string doubled = s + s; // Concatenate the string with itself

    // Print all rotations
    for (int i = n-1;i >=0;i--) {
        cout << doubled.substr(i+1, n) << endl;//right shift rotation from 1 place
    }

    return 0;
}
