#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin>>s;
    int n=s.size(),ans=0;
    for(int i=0;i<n;i++){
        int val=s[i];
        if(s[i]>=48 && s[i]<=57)
            ans+=val-'0';
    }
    
cout<<ans;
}
